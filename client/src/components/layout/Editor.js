import React from 'react'

const Editor = () => {

    //in state
    //add item1 + item2 + item3 = td 

    const onSelectorChange = (item) =>{
        //get item image & nutrients
        //change state
    }

    return (
        <div>
            
        </div>
    )
}

export default Editor
